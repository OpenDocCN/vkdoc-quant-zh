# 索引

## A

`Alpha Vantage API` 密钥 `TIME_SERIES_DAILY_ADJUSTED` 端点 `am` 函数 `摊销`，评估，函数化 `Anaconda`

## B

`银行` `比特币` `预算`，添加假期现金流，日期，现金流，现金流对象 `date_range` 函数 `.fillna(0)` 方法 月初，半月末 `fun` 对象，函数化，时间范围 `pd.date_range` 函数 `timestamp` `date-time.datetime` 对象 `totals` `cumsum()` `.tail` 方法 更新，可视化，原生 matplotlib `YAML`

## C

`加元兑美元`，转换，文档，封装 `.apply` `show_alternative` `openexchangerates.org` 机密 `Calendar` 对象 `加拿大元 (CAD)` 另请参见 `加元兑美元`，转换 `计算机编程`

## D, E, F, G, H

`数据` `日期`，日期格式化规则 `datetime.date` 对象 `datetime.datetime` 对象 `get_dates` 函数 `Python` `DatetimeIndex` 对象 `狗狗币` `IRR` `=IRR()` 不规则现金流时间表 挖矿 `pandas` `read_excel` 函数 `xirr` 函数 `xnpv` 函数 `ROI`

## I

`内部收益率 (IRR)` `投资组合` `deposit` 函数 设计，添加现金 `.at` 方法 `DataFrame` `instantiate_portfolio` 函数 可复用函数 `get_order` 函数 价格差距 `print(portfolio)` 函数 `.update` 方法 `rebalance` `simulate_process_order` 函数 `股票报价` 访问 `Alpha Vantage API` `get_historical` 函数 `get_price` 函数

## J, K, L

`Jupyter`

## M

`月初频率`

## N, O

`nteract`，空白状态，macOS `pip install`

## P, Q

`Pandas` `DataFrame`，`.from_dict` 代码 `Series` `Pandas 1.0` `支付循环` A，循环 B `个人投资组合` `Prophet`，定义，预测，`datestamp` 列 `.make_future_dataframe` 方法 数值列 `predict` 方法 `purchases` `purchases.csv` 文件 `可视化` `plot` 便捷方法 `plot_components` 方法 `Python`，整除运算符，预测库，编程

## R

`重复规则 (rrule)` `.between` 方法 `Recurrent 库`

## S

`半月末频率`

## T

`时间序列 (每日)` 键 `时间序列预测` `Timestamp`，归一化 `.normalize` 方法 `to_datetime` 函数

## U

`美元 (USD)` 另请参见 `加元兑美元`，转换

## V, W, X

`度假预算`

## Y, Z

`YAML` `Anaconda`，使用块加载 `totals`
