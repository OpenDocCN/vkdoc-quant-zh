# 互斥访问

## 互斥量

- 临界区
- `lock()` 和 `unlock()` 成员函数
- `Mutex` 和 `MutexAccess` 类，编码
- `MutexAccess`
- `pthread_mutex_lock`
- `pthread_mutex_unlock`
- `pthreads` API
- 同步机制
- `Mutex` 同步操作
- `MutexTestThread`

